{
  "name": "abhinav-yadav-profitlio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"npm:server\" \"npm run dev --prefix frontend\"",
    "server": "npm start --prefix backend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^10.0.5"
  }
}
